# Example environment variables for SIS project
# Copy this file to `.env` and fill in real credentials (do NOT commit `.env`).

# Database settings
DB_ENGINE=django.db.backends.mysql
DB_NAME=sis_db
DB_USER=sis
DB_PASSWORD=1234
DB_HOST=127.0.0.1
DB_PORT=3307

# Django settings
SECRET_KEY=replace-this-with-a-secure-secret
DEBUG=True
